module.exports = {
    'smartyEnergy': (browser) => {
        let login = browser.page.login()
        browser.resizeWindow(1920, 1080)
        login.navigate()
        
       
     
            
    }
}